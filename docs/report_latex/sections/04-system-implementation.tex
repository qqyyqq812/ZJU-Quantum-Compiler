\section{系统实现}

项目不仅实现了算法流程，还提供了面向使用者的多种调用方式。系统调用链路如图
\ref{fig:system-chain} 所示。网页、REST API、MCP 服务和命令行都可以触发同一
套编译流程，并返回路由后电路、SWAP 数、深度、耗时、轨迹和 SABRE 基线指标。

\begin{figure}[H]
  \centering
  \input{figures/system-chain.tikz}
  \caption{系统调用链路}
  \label{fig:system-chain}
\end{figure}

\subsection{接口层}

REST API 适合网页和普通后端调用。用户可以提交内置示例名称或自定义 OpenQASM 2
文本，系统返回结构化 JSON 结果。MCP 服务适合工具客户端和自动化场景，能够查询
状态、列出示例、运行 NPQR、运行 SABRE 基线并返回算法证据。命令行适合本地检查、
演示和快速基线矩阵生成。

这些入口的共同点是都把 NPQR 作为默认编译流程，同时把 SABRE 作为对比结果返回。
接口中的对比字段用于解释基线表现，不表示 NPQR 失败时会由 SABRE 替代完成。

\input{tables/interface_outputs.tex}

\subsection{编译层}

编译层负责把输入电路转化为路由任务，调用 NPQR 运行时，并将结果重新组织为用户
可读的输出。输出包括路由后 QASM、插入 SWAP 数、电路深度、运行时间和可复放轨迹。
轨迹字段使用户能够观察每一步 SWAP 和门执行，也便于报告中解释算法过程。

系统实现中有两条质量控制线。第一条是输入约束：只接受限定长度内的 OpenQASM 2
文本，并使用固定硬件拓扑进行演示，避免在线调用时出现不可控输入规模。第二条是
输出约束：返回结果必须带有完成状态、路由指标和轨迹信息。这样即使某个样例没有
得到理想指标，调用者也能判断结果是否完成、是否可复放、与基线差距在哪里。

\subsection{模型与证据层}

项目包含一个默认推理模型。该模型在搜索循环中提供动作偏好，不单独承担完整路由
生成。系统还提供机器可读的算法证据，说明默认路由、算法组成、SABRE basic 基线、
10/20 比特代表电路结果、30/50 比特扩展测试和能力边界。该证据用于保证报告、
网页和接口描述保持一致。

这种“模型 + 搜索 + 证据”的结构也降低了展示风险。报告可以明确说明神经网络在
动作评分环节发挥作用，同时承认最终路由质量还依赖搜索控制和修复机制。对在线
使用者而言，接口返回的是完整编译结果；对项目报告而言，证据层给出算法边界，避免
把单个样例结果扩大为未经验证的总体结论。
