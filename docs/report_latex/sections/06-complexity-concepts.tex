\section{复杂度与方法要点}

NPQR 的复杂度主要来自候选映射、候选 SWAP 展开、束搜索和局部修复。令 $n=|V|$
表示物理量子比特数，$m$ 表示电路门数，$e=|E|$ 表示硬件边数，$K$ 表示初始映射
候选数，$B$ 表示束宽，$D$ 表示主搜索最大展开深度，$A$ 表示每个状态保留的候选
动作数，$R$ 表示后缀修复深度。

\input{tables/complexity.tex}

复杂度表明，若不加剪枝，搜索空间会随深度指数增长。NPQR 通过固定束宽、动作
剪枝和有界修复把搜索规模限制在可控范围内。因此，该算法属于近似搜索框架：它
追求有限时间内的高质量可行解，而不保证全局最优。

从参数影响看，$B$ 和 $A$ 是主搜索成本的主要控制项。增大 $B$ 会保留更多候选路线，
有利于降低局部最优风险；增大 $A$ 会让每个状态尝试更多 SWAP，可能提高完成率。
但两者都会线性放大主搜索开销。修复深度 $R$ 的影响更强，因为局部修复最坏情况下
呈指数增长。因此，项目把修复限定在接近完成的状态上，而不是在整个搜索过程中
频繁启动。

\subsection{方法要点映射}

项目可以对应到量子信息基础大作业中的若干关键方法要点。表
\ref{tab:concept-mapping} 给出主要对应
关系。这种映射不是把每个经典算法完整复刻一遍，而是说明 NPQR 在问题建模和工程
实现中如何使用这些思想。

\input{tables/concept_mapping.tex}

\subsection{SABRE 的基线意义}

SABRE 的核心思想可以看作贪婪启发式与有限前瞻的结合。它根据前沿门和后续门在
物理拓扑上的距离选择 SWAP，通常能以较低时间成本给出稳定路线。NPQR 与 SABRE 的
区别在于：NPQR 引入神经网络动作评分，并在搜索中保留多条候选路线，同时加入局部
修复和轨迹复放。两者可以用于对比，但不能把 SABRE 结果作为 NPQR 的成功结果。

这一区别体现了神经启发式评分与经典搜索控制的结合。SABRE 更接近人工设计的
启发式函数，NPQR 则把启发式函数的一部分交给模型学习，再用显式搜索保证约束和
可解释性。二者的比较能说明：神经网络并不自动解决组合优化问题，合理的状态表示、
搜索边界和验证机制同样关键。
